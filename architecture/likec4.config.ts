import { defineConfig } from 'likec4/config'

export default defineConfig({
  name: 'mtogo-architecture',
  title: 'MToGo Architecture',
  exclude: ['**/node_modules/**', '**/dist/**', '**/.cache/**'],
})
